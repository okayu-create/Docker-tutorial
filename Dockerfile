# 1. ベースイメージの指定（Goのバージョン1.22を使用）
FROM golang:1.22

# 2. 作業ディレクトリ（調理場）の作成と移動
WORKDIR /app

# 3. ホストのファイルをコンテナ内にコピー
COPY . .

# 4. Goのプログラムをビルド（調理）
RUN go build -o main .

# 5. コンテナ起動時に実行するコマンド
CMD ["./main"]