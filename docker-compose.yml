services:
  web: # アプリの定義
    image: golang:latest
    tty: true # コンテナを起動状態のまま維持するための設定
    ports:
      - "8080:8080"
    environment:
      DB_HOST: db
    depends_on:
      - db

  db: # データベースの定義
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
